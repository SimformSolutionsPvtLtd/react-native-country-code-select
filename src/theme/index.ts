import {
  Metrics,
  moderateScale,
  verticalScale,
  horizontalScale,
} from './Metrics';
import { Icons } from '../assets';
import { Colors } from './Colors';

export {
  Metrics,
  moderateScale,
  verticalScale,
  Icons,
  Colors,
  horizontalScale,
};
